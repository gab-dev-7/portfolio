---
import Layout from '@layouts/Neovim.astro';
---

<Layout title="Help">
  <div class="flex flex-col items-center justify-center h-full">
    <h1 class="text-4xl mb-8 text-blue">Help</h1>
    <div class="text-center">
      <p class="mb-4">version 0.0.1</p>
      <div class="flex flex-col text-left justify-left p-10">
        <p class="p-4">
            This page contains basic instructions to use this page. Some Vim key bindings are enabled and can be used within the 
            page itself, but some of them don't work in all the pages.
        </p>

        <h2 class="p-4 text-2xl text-blue">Instructions</h2>
        <p class="ml-4"><a>u</a>: Undo the last change, in this case, it navigates backwards.</p>
        <p class="ml-4"><a>j</a>: Move the cursor down one line.</p>
        <p class="ml-4"><a>k</a>: Move the cursor up one line.</p>
        <p class="ml-4"><a>g</a>: Move the cursor to the first line of the file, in this case, page.</p>
        <p class="ml-4"><a>G</a>: Move the cursor to the last line of the file, in this case, page.</p>
      </div>
    </div>
  </div>
</Layout>
